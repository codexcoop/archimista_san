xml.catheader do
  xml.systemId PROVIDER
  xml.contact do
    xml.name "Codex"
    xml.mail "info@codexcoop.it"
  end
  xml.filedesc do
    xml.title "Export da Archimista a SAN"
    xml.date DateTime.now
  end
end